{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "prisma generate --schema=../../prisma/schema.prisma && (prisma migrate deploy --schema=../../prisma/schema.prisma || echo 'AgentTrace: no reachable DATABASE_URL at build — skipping migrations') && node vercel-build.mjs && mkdir -p public",
  "outputDirectory": "public",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }],
  "functions": {
    "api/index.ts": {
      "maxDuration": 15
    }
  }
}
